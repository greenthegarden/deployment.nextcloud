---

all:

  hosts:

    nextcloud-vm:
      ansible_connection: local
      ansible_host: localhost
      ansible_user: nextcloud
      public_iface: enp0s3

  vars:

  children:

    redis_instances:
      hosts:
        nextcloud-vm:

    postgres_instances:
      hosts:
        nextcloud-vm:

    nextcloud_instances:
      hosts:
        nextcloud-vm:
